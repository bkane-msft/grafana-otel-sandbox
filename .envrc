# Non-secret defaults for the sandbox. Override any of these and place secrets
# in .env (.gitignored and loaded last so .env wins).
export GRAFANA_URL=http://localhost:3000
export GRAFANA_ADMIN_USER=admin
export GRAFANA_ADMIN_PASSWORD=admin

dotenv
