apiVersion: 1

providers:
  - name: repo
    type: file
    folder: ''
    allowUiUpdates: true
    updateIntervalSeconds: 10
    options:
      path: /var/lib/grafana/dashboards
      foldersFromFilesStructure: true
